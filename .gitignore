docs/
.superpowers/

.DS_Store
__pycache__/
*.pyc

# Machine-specific: launch.json holds an absolute path to one developer's checkout.
.claude/

.vercel/
.worktrees/

# Where CI builds the inspection-only MSI. Inside the workspace so that
# upload-artifact roots the archive sensibly, which means it needs ignoring.
ci-artifacts/
