# Test-only dependencies. Kept out of the root requirements.txt so they never
# ship in the deployed function bundle.
#
# Install with:  pip install -r backend/requirements-dev.txt
-r ../requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
# cryptography moved to the root requirements.txt -- app/mfa.py now imports
# it at runtime (Fernet, for encrypting stored TOTP seeds). Still available
# here via the `-r ../requirements.txt` chain above.
pip-audit==2.7.3
