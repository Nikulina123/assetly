# Test-only dependencies. Kept out of the root requirements.txt so they never
# ship in the deployed function bundle.
#
# Install with:  pip install -r backend/requirements-dev.txt
-r ../requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
# Used only by backend/scripts/sign_release.py, which runs on a release
# owner's machine and never in CI or in the deployed function. Deliberately
# NOT in the root requirements.txt: the agents verify signatures with stdlib
# only, and the backend only ever reads an already-signed manifest.
cryptography==43.0.1
pip-audit==2.7.3
